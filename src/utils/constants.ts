// only for testing purpose in normal conditions should be for example passed as config/env or part of request when creating analyzer
export const BINANCE_API = 'https://testnet.binance.vision/api/v3';

// endpoint from which we extract historical trades
export const BINANCE_HISTORY_ENDPOINT = `${BINANCE_API}/aggTrades`;
